#### Description

Create a new SKILL.md template file in the current directory. The generated template includes YAML frontmatter with `name` and `description` fields and a markdown body for skill instructions.

#### Usage

```bash
aux4 ai skills init <name>
```

name   Name for the new skill (positional argument)

#### Example

```bash
aux4 ai skills init my-new-skill
```
